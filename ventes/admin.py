"""
Admin configuration for Sales Management
"""

from django.contrib import admin

# Since we're using MongoEngine, we don't need traditional Django admin
# But we can add admin documentation here for reference

# Order and OrderItem models are managed through MongoEngine
# Access them through MongoDB directly or create custom admin views if needed

