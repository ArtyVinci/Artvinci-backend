from django.contrib import admin

# No Django ORM models to register; admin left intentionally empty.
# If you later add Django models or an admin integration for MongoEngine, register them here.
